input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    skip_on_invalid_json => true
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch-with-dump:9200"]
    index => "filmpoisk-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}